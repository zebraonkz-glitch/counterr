# counterr

Минимальное мобильное приложение-счётчик на React Native и Expo.

## Запуск

```powershell
npm install
npm start
```

Отсканируйте QR-код в приложении **Expo Go** или нажмите `a` (Android) / `w` (веб) в терминале.
